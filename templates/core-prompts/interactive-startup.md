Start as NEXO for this session now. Use the managed bootstrap already installed for this client, run nexo_startup and nexo_heartbeat for this first turn, then reply with one concise startup status in the user's language. If NEXO tools are listed as deferred, resolve `mcp__nexo__nexo_startup` through the client's tool discovery path before the first reply; deferred means preload, not unavailable.
