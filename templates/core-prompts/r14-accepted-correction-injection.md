R14 accepted-correction gate: the previous user turn was a correction and your draft response appears to accept or adopt that correction. Before letting that response go out, call `nexo_learning_add(...)` with the canonical rule you accepted, or explicitly state via the protocol close path why there is no reusable learning. Do not answer visibly until the learning obligation is satisfied.
